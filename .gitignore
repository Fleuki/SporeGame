tools/.pixellab-token
tools/.rd-token

# Сборка для площадки: собирается из docs/ одной командой, в репозитории ей делать нечего
build/

# Playwright ставится отдельно и только тем, кто снимает скриншоты (tools/shots.mjs)
node_modules/
